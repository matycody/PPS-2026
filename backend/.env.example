# Puerto del servidor (por defecto 3001)
PORT=3001

# Base de datos Neon. Todavía no la usa el cronómetro.
# Reemplazar por la connection string real solo cuando se integre Prisma.
DATABASE_URL=postgresql://USUARIO:CONTRASENA@HOST/BASE?sslmode=require