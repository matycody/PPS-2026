{
  "scripts": {
    "postinstall": "prisma skills sync || exit 0",
	"dev": "node server.js"

  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.10.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
